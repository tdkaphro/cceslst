import { TraitementHistory } from './traitement-history.model';

describe('TraitementHistory', () => {
  it('should create an instance', () => {
    expect(new TraitementHistory()).toBeTruthy();
  });
});
