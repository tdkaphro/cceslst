import { TraitementMessage } from './traitement-message.model';

describe('TraitementMessage', () => {
  it('should create an instance', () => {
    expect(new TraitementMessage()).toBeTruthy();
  });
});
