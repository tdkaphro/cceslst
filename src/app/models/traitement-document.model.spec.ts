import { TraitementDocument } from './traitement-document.model';

describe('TraitementDocument', () => {
  it('should create an instance', () => {
    expect(new TraitementDocument()).toBeTruthy();
  });
});
