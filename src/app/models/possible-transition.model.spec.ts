import { PossibleTransition } from './possible-transition.model';

describe('PossibleTransition', () => {
  it('should create an instance', () => {
    expect(new PossibleTransition()).toBeTruthy();
  });
});
