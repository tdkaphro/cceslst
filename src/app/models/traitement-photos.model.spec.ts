import { TraitementPhotos } from './traitement-photos.model';

describe('TraitementPhotos', () => {
  it('should create an instance', () => {
    expect(new TraitementPhotos()).toBeTruthy();
  });
});
