import { TraitementComments } from './traitement-comments.model';

describe('TraitementComments', () => {
  it('should create an instance', () => {
    expect(new TraitementComments()).toBeTruthy();
  });
});
