import { AuthTokenResponse } from './auth-token-response.model';

describe('AuthTokenResponse', () => {
  it('should create an instance', () => {
    expect(new AuthTokenResponse()).toBeTruthy();
  });
});
