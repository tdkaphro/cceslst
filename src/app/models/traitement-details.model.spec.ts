import { TraitementDetails } from './traitement-details.model';

describe('TraitementDetails', () => {
  it('should create an instance', () => {
    expect(new TraitementDetails()).toBeTruthy();
  });
});
