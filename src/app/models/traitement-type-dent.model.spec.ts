import { TraitementTypeDent } from './traitement-type-dent.model';

describe('TraitementTypeDent', () => {
  it('should create an instance', () => {
    expect(new TraitementTypeDent()).toBeTruthy();
  });
});
